{
  "generation_params": {
    "comment": "Parameters for random block generation",
    "length_range": {
      "description": "Length quantization step",
      "step": 0.01,
      "comment": "Length will be snapped to this step (not grid)"
    },
    "width_range": {
      "description": "Width quantization step", 
      "step": 0.05,
      "comment": "Width will be snapped to this step (not grid)"
    },
    "multiplier_range": {
      "min": 8,
      "max": 32,
      "comment": "Even numbers only, range: [min, max]"
    },
    "num_fingers_range": {
      "min": 1,
      "max": 4,
      "comment": "Number of fingers per transistor"
    },
    "aspect_ratio": {
      "min_aspect_min": 0.2,
      "min_aspect_max": 0.8,
      "max_aspect_min": 1.2,
      "max_aspect_max": 2.0,
      "comment": "Random ranges for min and max aspect ratios"
    }
  },
  "design_constraints": {
    "comment": "Design-specific size constraints for each device type (designer-adjustable)",
    "nmos1v_nat": {
      "L": {
        "min": 0.4,
        "max": 2.0,
        "comment": "Length range for 1V NMOS (low-power analog typical)"
      },
      "W": {
        "min": 1.0,
        "max": 20.0,
        "comment": "Width range for 1V NMOS"
      }
    },
    "nmos2v_nat": {
      "L": {
        "min": 0.8,
        "max": 4.0,
        "comment": "Length range for 2V NMOS (power stages typical)"
      },
      "W": {
        "min": 1.4,
        "max": 30.0,
        "comment": "Width range for 2V NMOS"
      }
    },
    "pmos1v_nat": {
      "L": {
        "min": 0.4,
        "max": 2.0,
        "comment": "Length range for 1V PMOS (low-power analog typical)"
      },
      "W": {
        "min": 1.0,
        "max": 20.0,
        "comment": "Width range for 1V PMOS"
      }
    },
    "pmos2v_nat": {
      "L": {
        "min": 0.8,
        "max": 4.0,
        "comment": "Length range for 2V PMOS (power stages typical)"
      },
      "W": {
        "min": 1.4,
        "max": 30.0,
        "comment": "Width range for 2V PMOS"
      }
    }
  },
  "netlist": {
    "density": 1.5,
    "pins_per_block_min": 3,
    "pins_per_block_max": 5,
    "comment": "Netlist generation: N_nets = density * N_blocks, pins per block 3-6"
  },
  "validation": {
    "max_generation_attempts": 10,
    "comment": "Maximum attempts to generate a block with at least one valid variant"
  }
}